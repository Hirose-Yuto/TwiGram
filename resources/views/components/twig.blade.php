<div onclick="jump('{{$twig_url}}')" class="twig_at_home">
    <object>
        <a href="{{"/".$user->screen_name}}" class="user_at_twig">
            <span style="font-size: large">{{$user->user_name}}</span>
            {{"@".$user->screen_name}}
        </a>
    </object>
    {{$twig_how_long_ago}}
    <p>{!! nl2br(e($twig->program_result)) !!}</p>

    reply
    <form type=""
    retwig
    like
</div>
